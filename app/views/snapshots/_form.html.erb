<!-- TODO: use date/time picker -->
<%= text_field :taken_at, :label => "Taken At" %>

<%= hidden_field :name => :image_id, :value => snapshot.image.id %>
<%= image_tag snapshot.image.url, :class => 'snapshotimage' %>
<%= link_to "Change image", url(:new_whiteboard_image, snapshot.whiteboard, :existing_image_id => snapshot.image.id) %>

<%= text_area :body, :label => "Body (you may use Markdown)" %>
<%= check_box :public, :label => "Make Public" %>