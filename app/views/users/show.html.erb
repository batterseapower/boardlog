<h1>Users controller, show action</h1>

<p>Edit this file in <tt>app/views/users/show.html.erb</tt></p>
<p>For more information and examples of CRUD views read <a href="http://wiki.merbivore.com/howto/crud_view_example_with_merb_using_erb"> this wiki page</a>

<div class="breadcrumbs">
<%= link_to 'Users', resource(:users) %>
</div>

<%= partial :user, :with => @user %>

<% print user_is_user?; print "\n" * 3 %>

<% if user_is_user? then %>
  <div>
  <%= link_to 'Edit', resource(@user, :edit) %>
  <%= delete_button(@user, "Delete #{@user.name}") %>
  </div>
<% end %>